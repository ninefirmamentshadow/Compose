<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      No permissions are declared, and none should be added.

      Specifically absent, deliberately:
        android.permission.INTERNET          — there is no network code to serve
        ACCESS_FINE_LOCATION / COARSE        — the app has no use for where you are
        POST_NOTIFICATIONS                   — nothing here notifies
        SCHEDULE_EXACT_ALARM                 — nothing here schedules

      The only way text leaves this app is the clipboard, by an explicit tap.
    -->

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Drafts">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
